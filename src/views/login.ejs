<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./components/header'); %>
    <style type="text/css">
      .or-seperator {
        margin-top: 32px;
        text-align: center;
        border-top: 1px solid #e0e0e0;
      }
      .or-seperator i {
        padding: 0 8px;
        width: 30px;
        height: 30px;
        text-align: center;
        line-height: 26px;
        background: #f5f5f5;
        display: inline-block;
        position: relative;
        top: -15px;
        z-index: 1;
      }
    </style>
  </head>
  <body>
    <div class="access-wrapper d-flex align-items-center text-center" style="background-color: #f5f5f5">
      <main class="access-form w-100 m-auto">
        <form id="login-form" method="POST" action="/login">
          <img class="mb-4" src="/images/favicon/apple-touch-icon.png" alt="" width="72" height="72" />
          <h1 class="h3 mb-3 fw-normal">Please sign in</h1>

          <div class="form-floating">
            <input type="text" class="form-control" id="username" placeholder="Username" />
            <label for="floatingInput">Username</label>
          </div>
          <div class="form-floating">
            <input type="password" class="form-control" id="password" placeholder="Password" />
            <label for="floatingPassword">Password</label>
          </div>
          <!-- 
            <div class="checkbox mb-3">
              <label> <input type="checkbox" value="remember-me" /> Remember me </label>
            </div> -->
          <button class="w-100 btn btn-lg btn-primary" type="submit">Sign in</button>
          <a href="/register" class="text">Don't have account?</a>
        </form>
        <!-- 這是一條小分隔線，分隔線的中間有兩個字"OR"，這是用來分隔帳號密碼登入和第三方登入 -->
        <div class="or-seperator">
          <i>or</i>
        </div>
        <!-- 這是第三方登入的按鈕 -->
        <!-- <div class="d-flex justify-content-center">
            <a href="/auth/facebook" class="btn btn-primary btn-block">
              <i class="fab fa-facebook-f"></i> Sign in with Facebook
            </a>
          </div> -->
        <div class="d-flex justify-content-center">
          <a href="/login/google" class="btn btn-danger btn-block w-100">
            <i class="bi bi-google"></i> <span class="ms-2">Google</span>
          </a>
        </div>
      </main>
    </div>
    <%- include('./components/footer'); %>

    <script src="/javascripts/login/index.js"></script>
    <script src="/vendors/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
